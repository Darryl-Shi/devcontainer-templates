{
  "name": "tinygrad",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  "runArgs": [],
  "containerEnv": {},
  "forwardPorts": [8888, 5000, 8000],
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "charliermarsh.ruff",
        "ms-python.black-formatter"
      ]
    }
  },
  "postCreateCommand": "python - <<'PY'\nimport tinygrad, numpy\nprint('tinygrad', tinygrad.__version__)\nPY",
  "remoteUser": "root",
  "updateRemoteUserUID": true
  ,
  "workspaceFolder": "/workspaces/tinygrad",
  "initializeCommand": "/bin/sh -lc 'if command -v selinuxenabled >/dev/null 2>&1 && selinuxenabled; then chcon -Rt container_file_t \"${localWorkspaceFolder}\" || true; fi'"
}
